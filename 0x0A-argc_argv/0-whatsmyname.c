#include <stdio.h>

/**
 *main - printf the name of the main program
 *@argc: the counter of argument
 *@argv: array of variables commands
 *Return: (0);
 */

int main(int __attribute__((__unused__)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
