#include <stdio.h>
#include "main.h"
#include <string.h>

/**
 *_strlen - is a func count the number of element in an string
 *
 *@s: is a string pointer
 */

int _strlen(char *s)
{
	return(strlen(s));
}
