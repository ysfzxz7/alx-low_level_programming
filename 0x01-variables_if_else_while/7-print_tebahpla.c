#include <stdio.h>
/**
 *main - Entry point
 *
 *Return: Always 0 (Success);
 */

int main(void)
{
	int chaar;

	for (chaar = 122; chaar > 96; chaar--)
		putchar(chaar);
	putchar('\n');
return (0);
}
